class hero():
    def name(self,names, gender):
        self.gender = gender
        self.names = names
        print (self.gender)
        print (self.names)
        
# ?(object-name) = (class-name)
obj = hero()  
(obj.name('dipen','male'))   